// Injected by electron-vite `define` (see electron.vite.config.ts). true only in
// a `npm run package:dev` build; false (and dead-code-eliminated) in release.
declare const __AIRLOCK_DEV_UPDATE__: boolean;
