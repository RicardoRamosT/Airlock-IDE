<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Electron defaults (hardened runtime). -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<!-- Microphone access so Claude Code's /voice dictation can capture audio
	     when Claude runs in AirLock's embedded terminal. Required under the
	     hardened runtime; the Info.plist already carries NSMicrophoneUsageDescription. -->
	<key>com.apple.security.device.audio-input</key>
	<true/>
</dict>
</plist>
